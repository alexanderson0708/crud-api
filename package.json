{
  "name": "crud-api",
  "version": "1.0.0",
  "description": "",
  "main": "index.ts",
  "scripts": {
    "start:dev": "cross-env NODE_ENV=development NODE_NO_WARNINGS=1 nodemon",
    "start:prod": "cross-env NODE_ENV=production npm run build && node ./dist/bundle.js",
    "start:dev:multi": "cross-env MODE=cluster NODE_NO_WARNINGS=1 nodemon",
    "start:prod:multi": "cross-env NODE_ENV=production npm run build && cross-env MODE=cluster node ./dist/bundle.js",
    "build": "webpack",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^16.0.3",
    "uuid": "^9.0.0"
  },
  "devDependencies": {
    "@types/jest": "^29.5.1",
    "@types/node": "^18.16.0",
    "@types/supertest": "^2.0.12",
    "@types/uuid": "^9.0.1",
    "@types/webpack": "^5.28.1",
    "cross-env": "^7.0.3",
    "nodemon": "^2.0.22",
    "ts-loader": "^9.4.2",
    "ts-node": "^10.9.1",
    "typescript": "^5.0.4",
    "webpack-cli": "^5.0.2"
  }
}
